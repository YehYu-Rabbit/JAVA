1. 傳直呼叫(call by value)  
指定另一個變數指向這個值時，電腦會在記憶體中新增(複製)一個新值，讓後來的這個變數指向新的值。
2. 參考呼叫(call by reference)
指定另一個變數指向這物件，這個變數就會指向電腦記憶體中間同樣的物件，部會有心的物件在記憶體中被創造出來。
3. 方法多載(Overloading)
是指多個名稱相同但參數個數或型別不同得方法，編譯器依傳入參數的個數，型別與順序決定使用哪一種方法。
